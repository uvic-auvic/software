<launch>
    <node name="vision"
          pkg="vision"
          type="vision_node"
          required="true"
          output="screen">
        <!-- Arugments that may or may not be required go here. -->
    </node>
</launch>